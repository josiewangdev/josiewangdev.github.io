---
layout: post
title: 导航栏闪变黑色 · Navigation bar flash to black
tags: iOS
categories: Dev
---

**Problem Description:**

After setting the navigation bar is hidden, the navigation bar color will flash to black when push it.

**Solution:**

When set navigation bar hidden, set `animated` as `true`.

**问题描述:**

设置Navigation Bar隐藏后，再push时发现导航栏会闪变黑色。

**解决方法:**

在设置导航栏隐藏时，将 `animated` 值设为 `true` .

---

{% highlight swift %}
navigationController.setNavigationBarHidden(true, animated: true)
{% endhighlight %}